# Collab Testing Libraries

## Structure
- `python/`
- `c`
- `cpp`
- `esp32` — ESP32 firmware (PlatformIO, Arduino framework)
- `esp32-idf` — ESP32 firmware (PlatformIO, ESP-IDF framework) — in-progress
  port of `esp32/`, kept as a separate project during migration
